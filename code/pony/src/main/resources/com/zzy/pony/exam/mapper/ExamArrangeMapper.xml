<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zzy.pony.exam.mapper.ExamArrangeMapper">
	<select id="timeList" resultType="map">
		SELECT distinct DATE_FORMAT(exam_date,'%Y-%m-%d') examDate,DATE_FORMAT(start_time,'%H:%i') startTime,DATE_FORMAT(end_time,'%H:%i') endTime 
 		FROM t_exam_arrange where exam_id=#{0} order by exam_date,start_time
	</select>
</mapper>